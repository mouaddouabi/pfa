import { FicheConditionnement } from './fiche-conditionnement';

describe('FicheConditionnement', () => {
  it('should create an instance', () => {
    expect(new FicheConditionnement()).toBeTruthy();
  });
});
